# Plotter2D

## __init__

Initialize a new Plotter instance.

```python
def __init__(
    self,
    title: str,
    x_min: float,
    x_max: float,
    y_min: float,
    y_max: float,
    feed_rate: float,
    handle_out_of_bounds: THandleOutOfBounds,
    output_directory: str = "./output",
)
```
